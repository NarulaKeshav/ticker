$(document).ready(function() {

    // Initializing WOW JS
    new WOW().init();
    var d = document;
    var stopwatchTimer = d.getElementById("stopwatch-timer");
    var start = d.getElementById("btn-start");

    // Stopwatch

});